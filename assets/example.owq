let str = "abcdefgh" + "efghefgh";
let imax = 1024 / str.length() * 16 * 4 * 2; // 65536

let i = 0;
let lngth;

// will loop 65536 times:
while (i < imax) {
    lngth = str.length() * i;
	i = i + 1;
}

print("Iterations: " + i + " - Result: " + lngth);