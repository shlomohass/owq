let a = null,
	b = NULL,
	c, d;

c = a =~ b;
d = a == b; // Null is not a value and cannot be compared or claculated

print(a.isNull() + ","); // TRUE
print(a.type() + ",");   // NULL
print(b.isNull() + ","); // TRUE
print(b.type() + ",");   // NULL
print(c); // TRUE
print(d); // FALSE